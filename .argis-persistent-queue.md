# Argis Persistent Queue

Items tagged `--!!(#N)>` are carried across `/loop` fires until resolved or explicitly dismissed. Each `/loop` tick reads this file, surfaces items that match current state, and acts on any that are executable without blocking on user input.

Format per line: `--!!(#N)> STATUS category — description`

Status:
- `OPEN` — needs action or answer
- `WAITING` — dispatched; parked until external event
- `DONE` — resolved; kept briefly for audit then purged

Category examples: `user-input`, `provision`, `merge`, `dispatch`, `verify`, `question`.

---

## Active queue (2026-04-24)

--!!(#1)> WAITING provision — OCI Ampere capacity poller PID 28055; retries every 5min across PHX-AD-{1,2,3} until Always-Free shape frees up. Log /tmp/oci-capacity-poller.log. No action needed until it lands.

--!!(#2)> OPEN user-input — Windows desktop `kooshapari-desk.tail2b570.ts.net` (Windows 11 x86): provide user account for Gitea Actions runner (`forgejo-runner` dedicated OR regular user?) so I can generate the PowerShell install script once Forgejo is up.

--!!(#3)> OPEN user-input — Tailscale admin API key needed (to mint ephemeral auth-keys for OCI/GCP VMs + CI runners). Generate at https://login.tailscale.com/admin/settings/keys. Paste as `export TS_API_KEY=tskey-api-...`

--!!(#4)> OPEN user-input — AWS key rotation: admin key in `~/.aws/credentials` since 09:37Z; rotate within 24h.

--!!(#5)> OPEN question — GCS vs CF R2 for artifact CDN long-term: GCS is up now; R2 is blocked on CF payment decline. Switch back to R2 once CF payment resolves? Yes/no.

--!!(#6)> OPEN decision — pheno/PhenoLang traceability strict-checker (task #168): pick (A) backfill 46 annotations, (B) soft-fail + track, or (C) admin-merge CVE PRs and defer.

--!!(#7)> WAITING merge — Tier-1 tech-debt chain on AgilePlus: #364 → #365 → #366 → #367 → #368 → #369 → #21 (AuthKit#21 standalone). All stacked + ready. Admin-merge #364 first, rest cascade.

--!!(#8)> OPEN user-input — 107/138 session PRs are admin-merge-safe per #157 audit; batch-run `worklogs/GOVERNANCE.md` "Ready-for-admin-merge" one-liner block when ready.

--!!(#9)> WAITING cloud-schedule — Remote routine `trig_01WyavEsSwm8ouHmtNTD7E7k` fires Sun 2026-04-26 17:07Z to draft observability triad on phenotype-tooling (task #67). Fails fast if `phenotype-tooling` repo doesn't exist on GitHub. **Create it before Sun** via `gh repo create KooshaPari/phenotype-tooling --public --source /Users/kooshapari/CodeProjects/Phenotype/repos/phenotype-tooling --push` OR the routine will emit "blocked" output.

--!!(#10)> OPEN user-input — `phenotype-infra` local scaffold (#110, 58 files, 2492 LOC) ready at `repos/phenotype-infra/SETUP_STEPS.md`. Run:
```
cd /Users/kooshapari/CodeProjects/Phenotype/repos/phenotype-infra
git init -b main && git add . && git commit -m "feat: initial phenotype-infra scaffold"
gh repo create KooshaPari/phenotype-infra --public --source . --push
```

--!!(#11)> OPEN question — `kooshapari-desk.tail2b570.ts.net` OS/arch for runner binary selection: Windows 11 x86 (confirmed by user). CPU arch: x86_64 assumed. AMD64 build of act_runner.exe. Correct?

--!!(#12)> WAITING provision — Vercel `preview.kooshapari.com` TLS pending. Vercel issues cert ~5-15min after DNS A record lands (written 09:37Z). Check: `curl -sI https://preview.kooshapari.com 2>&1 | head -3`. Should return 200 or 404 (not TLS handshake failure) once live.

--!!(#13)> OPEN decision — Tracera:main vs kept-separate (task #72): the `feat/phenokits-subtree-import-20260423` branch on KooshaPari/Tracera has extracted content but no PR to main. Full subtree replace / sidecar / cherry-pick?

--!!(#14)> OPEN decision — PhenoAgent + PhenoSchema destructive cleanup (task #76): local canonical dirs with no remote counterpart; delete or preserve?

--!!(#15)> OPEN decision — Helios's FocalPoint/target is 44Gi on your disk. Not mine to touch, but if you want I can ask Helios (via .argis-helios-bus/) to purge it — would free substantial disk for agent work.

---

## Resolved (last 10)

(will fill as items close)
