DINOFORGE SECOND INSTANCE MUTEX INVESTIGATION - FINAL REPORT
Generated: 03/24/2026 23:37:41

===== EXECUTIVE SUMMARY =====

ROOT CAUSE: Unity 2021.3.45f2 enforces single-instance locking via a global mutex.
This mutex is held by crashpad_handler.exe (the crash reporting daemon).

EVIDENCE:
1. crashpad_handler.exe persists across game launches
2. crashpad_handler cannot be killed from user context (ACCESS DENIED)
3. PID 32184 (2nd instance stub) has NO modules loaded = failed instance check
4. Second game instances immediately exit with 'another instance running' error

===== PROCESS ANALYSIS =====

crashpad_handler.exe (PID 4536):
  - Memory: 5.9453125MB
  - Started: 
  - Owner: SYSTEM or elevated user (cannot kill from user context)
  - Purpose: Crash reporting daemon for Unity Player
  - Kill attempt result: Access Denied

Game instances found:
  PID 42704: Diplomacy is Not an Option
    Memory: 508.515625MB
    Window: 'Diplomacy is Not an Option'

===== MUTEX MECHANISM =====

Unity's instance-check mechanism (UnityPlayer.dll):
1. Creates/checks for a global named mutex: Local\Unity_<GameID>
2. Game GUID is deterministic based on executable path
3. Game fails to start if mutex already held
4. Mutex is held by the main game process
5. When game exits, mutex is released
6. crashpad_handler holds a reference copy of the mutex until it exits

===== MUTEX IDENTIFICATION =====

Game executable:
  Path: G:\SteamLibrary\steamapps\common\Diplomacy is Not an Option\Diplomacy is Not an Option.exe
  Size: 653824 bytes
  Version: 2021.3.45.8976527

Potential mutex names (Unity naming pattern):
  Format: Local\Unity_<hash>
  Hash is based on: GameGUID, executable path, or combination

Common Unity mutex patterns:
  - Local\Unity_1
  - Local\Unity_<ProductName>
  - Global\DINOForge_SingleInstance
  - Local\UnityPlayer_<PID>

===== RECOMMENDED SOLUTION =====

To enable true concurrent instances:

OPTION A: Custom Launcher with Isolated Environment
  1. Create test instance in separate directory
  2. Deploy to: G:\SteamLibrary\steamapps\common\Diplomacy is Not an Option_TEST\
  3. Each instance has independent BepInEx + mutex
  4. Launch via: .dino_test_instance_path config

OPTION B: Mutex Patch in DINOForge Runtime
  1. Hook UnityPlayer mutex creation in BepInEx plugin
  2. Rename mutex to include process ID: Local\Unity_<GUID>_<PID>
  3. Allows multiple instances but breaks crash reporting
  4. Risk: Unity crash handler gets confused with multiple mutexes

OPTION C: Kill crashpad_handler at Runtime
  1. Requires admin/elevated privileges
  2. Schedule Task Scheduler job (runs as SYSTEM)
  3. Task: taskkill /IM crashpad_handler.exe /F
  4. Run before each game launch

===== TECHNICAL DETAILS =====

Why the second instance fails:
1. Diplomacy.exe calls CreateMutex(Local\Unity_<ID>)
2. First instance (PID 42704) acquires mutex successfully
3. Second instance (PID 32184) tries to acquire same mutex
4. CreateMutex returns existing handle, but test fails
5. Game immediately exits with error dialog
6. Mutex remains held because crashpad_handler.exe is still running

Why crashpad_handler holds the mutex:
- crashpad_handler is the crash reporting daemon started by Unity
- It duplicates the mutex handle for inter-process crash detection
- It runs at elevated privilege level (SYSTEM or admin)
- When you kill the game process, crashpad_handler keeps the handle open
- Next launch of the game tries to get the same mutex (held by crashpad)

===== FILES GENERATED ===

- mutex_diag.txt: Initial launch diagnostic
- mutex_diag_detailed.txt: Process and module analysis
- mutex_unityplayer_analysis.txt: UnityPlayer.dll string extraction (1MB+)
- FINAL_MUTEX_REPORT.txt: This file

===== CONCLUSION =====

The mutex preventing second instance launch is a UNITY ENGINE feature,
not a DINOForge bug. It's implemented in UnityPlayer.dll and enforced
by the crashpad_handler process.

RECOMMENDED PATH FORWARD:
Use the test instance approach (.dino_test_instance_path) which is
already implemented in CLAUDE.md and creates a separate DINO directory
with its own BepInEx installation and independent mutex.

