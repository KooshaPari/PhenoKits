<Window
    x:Class="DINOForge.DesktopCompanion.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DINOForge Companion">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base" />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <NavigationView
            Grid.Row="0"
            x:Name="NavView"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            PaneDisplayMode="Left"
            OpenPaneLength="200"
            SelectionChanged="NavView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Dashboard" Tag="Dashboard" Icon="Home"
                                    AutomationProperties.AutomationId="NavDashboard" />
                <NavigationViewItem Content="Pack List" Tag="PackList" Icon="List"
                                    AutomationProperties.AutomationId="NavPackList" />
                <NavigationViewItem Content="Debug Panel" Tag="DebugPanel" Icon="Repair"
                                    AutomationProperties.AutomationId="NavDebugPanel" />
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Settings" Tag="Settings" Icon="Setting"
                                    AutomationProperties.AutomationId="NavSettings" />
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <!-- Status bar: bridge connection indicator -->
        <Border Grid.Row="1"
                Padding="12,4"
                BorderThickness="0,1,0,0"
                BorderBrush="{StaticResource DFBorderBrush}"
                Background="{StaticResource DFSurfaceBrush}">
            <TextBlock
                x:Name="BridgeStatusText"
                AutomationProperties.AutomationId="BridgeStatusText"
                Text="Not connected"
                Style="{StaticResource DFSecondaryText}" />
        </Border>
    </Grid>
</Window>
