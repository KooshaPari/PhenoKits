using Avalonia.Controls;

namespace DINOForge.Installer.Views;

/// <summary>
/// Progress / completion page — shows the install log and post-install actions.
/// </summary>
public partial class ProgressPage : UserControl
{
    public ProgressPage()
    {
        InitializeComponent();
    }
}
