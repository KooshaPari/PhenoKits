//! Infrastructure Layer
//!
//! This module exists to keep the crate layout consistent while the concrete
//! adapters are still under development.

/// Initialize infrastructure subsystems (noop until adapters land).
pub fn initialize() {}
