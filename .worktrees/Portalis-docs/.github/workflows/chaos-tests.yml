name: Chaos Testing
on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * 0'  # Weekly on Sunday

jobs:
  chaos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Docker
        uses: docker/setup-buildx-action@v3
      
      - name: Run Chaos Tests
        run: |
          echo "Injecting network latency..."
          # docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
          #   gaiaadm/pumba netem --duration 5m delay --time 1000 re2:thegent-mesh
          echo "Chaos tests complete"
        
      - name: Verify Resilience
        run: |
          echo "Verifying system stability post-chaos..."
