# Research

- Existing docs tree already contains `docs/sessions/index.md` but no canonical session artifact bundle.
- The docs site is VitePress-based, so session docs should be plain markdown files under `docs/sessions/<session>/`.
