name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: KooshaPari/template-commons/.github/workflows/reusable-python-ci.yml@main
    with:
      python-version: '3.11'
      package-name: phenotype-agent-core
