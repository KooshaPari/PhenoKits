# Archive

`phenotype-nexus` is archived. Active development has moved to `libs/nexus` in the main
Phenotype workspace monorepo.

## Migration Notes

- The standalone repo is preserved for historical reference.
- New development should happen in the monorepo copy.
- Existing consumers can continue to pin the archived repository if needed.
