# Template Commons - Shared workflow templates
# Native task runner (just)

# Default recipe
default: help

# Help
help:
  @echo "Template Commons - Shared workflow templates"
  @echo ""
  @echo "Available recipes:"
  @just --list

# Quality checks
check: fmt clippy test
  @echo "All checks passed!"

# Format code
fmt:
  cargo fmt --all

# Lint
clippy:
  cargo clippy --workspace -- -D warnings

# Run tests
test:
  cargo test --workspace

# Build
build:
  cargo build --workspace --release

# Clean
clean:
  cargo clean --workspace

# Documentation
doc:
  cargo doc --workspace --no-deps --all

# Benchmarks
bench:
  cargo bench --workspace
