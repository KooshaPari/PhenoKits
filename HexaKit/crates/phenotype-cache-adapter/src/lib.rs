//! Phenotype Cache Adapter
//!
//! Provides caching abstractions and implementations for the Phenotype ecosystem.

/// Placeholder type for cache adapter
pub struct CacheAdapter;

impl CacheAdapter {
    /// Create a new cache adapter
    pub fn new() -> Self {
        Self
    }
}

impl Default for CacheAdapter {
    fn default() -> Self {
        Self::new()
    }
}
