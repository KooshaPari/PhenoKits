# stashly-migrations

SQLite migration runner for schema management and versioning.

**Features:**
- Automatic migration discovery
- Version tracking
- Rollback support
- Async operations via sqlx

**Tests:** Inline `#[test]` in `src/lib.rs`

**Status:** Stub (scaffolding only)
