[package]
name = "phenotype-errors"
version = "0.1.0"
edition = "2021"
authors = ["Phenotype Org"]
license = "MIT OR Apache-2.0"
description = "Canonical error types for Phenotype collections"

[dependencies]
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }
